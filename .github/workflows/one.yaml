name: testing

on: push

jobs: 
    test-job:
        runs-on: ubuntu-latest
        steps:
            - name: Hello Disply
              run: echo "welcome to Github Actions"
            
            - name: Display the date
              run: date