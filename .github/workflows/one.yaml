name: testing

on: 
    # push:
    workflow_dispatch: 

jobs: 
    test-job:
        runs-on: ubuntu-latest
        steps:
            - name: Hello Disply
              run: echo "welcome to Github Actions"
            
            - name: Display the date
              run: date
